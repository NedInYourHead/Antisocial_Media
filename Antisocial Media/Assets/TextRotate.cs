using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*public class TextRotate : MonoBehaviour
{
    [SerializeField] private float rotation = 0f;
    [SerializeField] private float minRot = -30f;
    [SerializeField] private float maxRot = 30f;
    [SerializeField] private float rotSpeed = 1f;

    void FixedUpdate()
    {
        rotation += rotSpeed * Time.deltaTime;
        if (rotation > maxRot)
        {
            rotSpeed *= -1;
        }
        else if (rotation < minRot)
        {
            rotSpeed *= -1;
        }
        GameObject playerRot = GameObject.GetComponent<>);
        transform.rotation = Quaternion.Euler(new Vector3(0.0f, playerRot, rotation));*/
    }
}
